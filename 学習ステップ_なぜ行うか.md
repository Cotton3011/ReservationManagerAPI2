# ReservationManagerAPI2 学習メモ: なぜ各ステップを行うのか

このメモは、各ステップの目的を振り返るためのものです。
「何を作ったか」ではなく、「なぜ必要か」「行わないと何が困るか」に焦点を当てます。

## 各ステップで具体的に行ったこと

### API基礎

| Step | 具体的に行ったこと |
| --- | --- |
| 1. Entity と Role enum | `User`、`Reservation`、`UserRole`、`ReservationStatus` を作成し、予約とユーザーの関係・状態・権限を定義した。 |
| 2. EF Core と Migration | `AppDbContext`、`DbSet`、`OnModelCreating` を設定し、Migration を作成して SQL Server にテーブルを作成した。 |
| 3. 認証サービス | 登録・ログイン用DTO、`AuthService`、`PasswordHasher` を実装し、パスワードをハッシュ化して保存するようにした。 |
| 4. JWT認証と認可 | `JwtService`、`AddAuthentication`、`AddJwtBearer`、`[Authorize]` を設定し、SwaggerからBearerトークンで呼び出せるようにした。 |
| 5. 予約管理 | `ReservationService` に日時検証、過去日時拒否、重複予約判定、一覧・詳細・キャンセル処理を実装した。 |
| 6. Admin機能 | `AdminController` を作成し、Adminだけが全予約の取得と任意予約のキャンセルをできるようにした。 |
| 7. 例外・ログ・単体テスト | `ExceptionHandlingMiddleware`、業務例外、`ILogger`、xUnitの`ReservationServiceTests`を追加した。 |

### Docker / CI

| Step | 具体的に行ったこと |
| --- | --- |
| 1. Docker基礎 | Image、Container、Dockerfile、Compose、port、volume、environment、depends_onの役割を確認した。 |
| 2. APIのコンテナ化 | .NET 8用のマルチステージ`Dockerfile`と`.dockerignore`を作成し、`docker build`と`docker run`でSwaggerを開いた。 |
| 3. SQL Serverコンテナ | SQL Server 2022 Imageを取得し、`MSSQL_SA_PASSWORD`、`ACCEPT_EULA`、ポート`1433`でDBコンテナを起動した。 |
| 4. Docker Compose | `docker-compose.yml`に`api`と`db`サービス、healthcheck、volume、`depends_on`を設定し、`docker compose up --build`で同時起動した。 |
| 5. 環境変数管理 | `.env`と`.env.example`を用意し、DB接続文字列、JWT設定、初期Admin情報を環境変数から渡すようにした。 |
| 6. GitHub Actions build | `.github/workflows/ci.yml`を作成し、push時に`dotnet restore`と`dotnet build`を実行するようにした。 |
| 7. GitHub Actions test | テストプロジェクトを`tests`配下へ整理し、CIに`dotnet test`を追加して自動テストを有効にした。 |
| 8. 例外処理Middleware | `400`、`404`、`409`、`500`などを`ProblemDetails`形式で返す例外処理を確認した。 |
| 9. ILogger | 予約作成、重複予約、キャンセル、例外を`ILogger`で記録し、Docker ComposeとDocker Desktopのログで確認した。 |
| 10. README整理 | Docker起動、環境変数、API一覧、ER図、テスト、例外処理、ログをREADMEへ整理した。 |

### Azure

| Step | 具体的に行ったこと |
| --- | --- |
| 1. Azure基礎 | Azureサブスクリプションを確認し、Japan Eastに`rg-reservation-manager-japaneast`リソースグループを作成した。 |
| 2. コスト管理 | 予算を作成し、Azure SQL Databaseの無料プランと超過請求無効を確認し、Container Appsの最小レプリカを`0`にした。 |
| 3. Azure SQL Database | サーバーレスAzure SQL Database`reservationmanagerdb`とSQL認証用サーバーを作成した。 |
| 4. Azure SQL Migration | User SecretsへAzure SQL接続文字列を設定し、`dotnet ef database update`でMigrationを適用してテーブルを作成した。 |
| 5. Docker Hub | Docker Hubリポジトリ`cotton3011/reservation-manager-api`を作成し、Imageのタグ付けとpushを行った。 |
| 6. Container Apps公開 | `ca-reservation-manager-api`を作成し、公開HTTP ingress、ポート`8080`、Container Appsのシークレットと環境変数を設定した。 |
| 7. Application Insights | Azure Monitor OpenTelemetryを追加し、Application Insights、Log Analytics、`AppRequests`、`AppDependencies`、コンテナログを確認した。 |
| 8. GitHub Actions CI/CD | OIDC用アプリ登録、フェデレーション資格情報、Azureロール、GitHub Secretsを設定し、Docker pushとContainer Apps更新を自動化した。 |
| 9. Azure README | Azure構成図、シークレット方針、Migration、監視、CI/CD、コスト管理をREADMEへ追記した。 |

## API基礎

### Step 1: Entity と Role enum

**なぜ行うのか**

アプリケーションが扱うユーザー、予約、予約状態、権限を明確な型として定義するためです。`User` と `Reservation` の関係、`User` と `Admin` の違いをコード上で一貫して扱えます。

**行わないとどうなるか**

ユーザー権限や予約状態を文字列や数値で場当たり的に扱うことになり、入力ミス、状態の不整合、権限チェック漏れが起きやすくなります。

### Step 2: EF Core と Migration

**なぜ行うのか**

C# のEntity定義とデータベースのテーブル構造を対応させ、変更履歴をMigrationとして管理するためです。チームや環境ごとに同じDB構造を再現できます。

**行わないとどうなるか**

手作業でテーブルを作る必要があり、開発者ごと・環境ごとにDB構造がずれます。デプロイ時に必要なテーブルや列がない問題が起きます。

### Step 3: 認証サービス

**なぜ行うのか**

登録、パスワードハッシュ化、ログイン判定という業務ロジックを`AuthService`へ集約するためです。ControllerをHTTPの入口に保ち、認証処理をテストしやすくします。

**行わないとどうなるか**

パスワードを平文で保存する、Controllerごとに認証処理が重複する、ログイン判定が統一されない、といった重大なセキュリティ・保守問題につながります。

### Step 4: JWT認証と認可

**なぜ行うのか**

ログイン済みの利用者をリクエストごとに識別し、UserとAdminで許可する操作を分けるためです。サーバー側でログイン状態を保持せずにAPIを安全に利用できます。

**行わないとどうなるか**

誰でも他人の予約を取得・キャンセルできる状態になります。管理者専用の機能も保護できません。

### Step 5: 予約管理

**なぜ行うのか**

開始・終了日時の検証、過去日時の拒否、時間帯の重複防止、キャンセル状態の管理を、予約の業務ルールとして一箇所に置くためです。

**行わないとどうなるか**

終了日時が開始日時より前の予約や、同じ時間帯の二重予約が登録されます。予約データを信頼できなくなります。

### Step 6: Admin機能

**なぜ行うのか**

一般ユーザーの「自分の予約だけ操作できる」という制約を守りつつ、管理者には全予約を確認・管理する責務を与えるためです。

**行わないとどうなるか**

一般ユーザーと管理者の権限が混ざり、情報漏えいまたは運用担当者が必要な管理操作をできない問題が起きます。

### Step 7: 例外・ログ・単体テスト

**なぜ行うのか**

業務上想定できる失敗をHTTPステータスへ正しく変換し、問題発生時の記録を残し、重要な業務ルールを自動テストで守るためです。

**行わないとどうなるか**

API利用者には原因不明の500エラーが返り、運用者は何が起きたか追跡できません。将来の修正で重複予約判定などが壊れても気づきにくくなります。

## Docker / CI

### Step 1: Docker基礎

**なぜ行うのか**

Image、Container、Dockerfile、Compose、volume、environmentの役割を理解し、ローカルと本番の実行環境を揃える基礎を作るためです。

**行わないとどうなるか**

「自分のPCでは動くが、他のPCやサーバーでは動かない」という環境差異の問題を解決できません。

### Step 2: APIのコンテナ化

**なぜ行うのか**

.NET APIをDocker Imageとしてパッケージ化し、どこでも同じ手順で実行できるようにするためです。

**行わないとどうなるか**

サーバーごとに.NET SDKや依存関係を手作業で設定する必要があり、環境差異とデプロイミスが増えます。

### Step 3: SQL Serverコンテナ

**なぜ行うのか**

開発用DBもコンテナとして起動し、APIと同じように再現可能なローカル環境にするためです。volumeによってデータの永続化も理解します。

**行わないとどうなるか**

PCごとにSQL Serverの設定が異なり、DBの準備や接続確認に時間がかかります。コンテナ再作成でデータを失う可能性もあります。

### Step 4: Docker Compose

**なぜ行うのか**

APIとDBの複数コンテナを、ネットワーク、起動順、環境変数、volumeを含めて一つの構成として管理するためです。

**行わないとどうなるか**

複数の`docker run`コマンドを毎回手作業で実行することになり、ポート、接続先、起動順の設定漏れが起きます。

### Step 5: 環境変数管理

**なぜ行うのか**

DB接続文字列、JWT秘密鍵、初期管理者情報などをコードから分離し、環境ごとに安全に切り替えるためです。

**行わないとどうなるか**

秘密情報がGitへ混入し、漏えい時にすべての履歴から消す必要が出ます。本番・開発で接続先を切り替えることも困難になります。

### Step 6: GitHub Actions build

**なぜ行うのか**

pushしたコードが、開発者のPC以外でも依存関係を復元してビルドできるかを自動検証するためです。

**行わないとどうなるか**

コンパイルできないコードがmainブランチへ入り、他の開発者やデプロイ環境で初めて問題が発覚します。

### Step 7: GitHub Actions test

**なぜ行うのか**

ビルドだけでは確認できない業務ルールを、pushごとに自動で検証するためです。

**行わないとどうなるか**

予約重複判定のような機能が修正で壊れても、手動確認まで検知できません。

### Step 8: 例外処理Middleware

**なぜ行うのか**

ControllerやServiceで起きた例外を一箇所で捕捉し、統一したHTTPエラー形式に変換するためです。

**行わないとどうなるか**

Controllerごとにエラー処理が重複し、同じ失敗でも返却形式やステータスコードがばらばらになります。

### Step 9: ILogger

**なぜ行うのか**

予約作成、重複、キャンセル、例外のような重要イベントをログとして残し、実行中のシステムを調査できるようにするためです。

**行わないとどうなるか**

本番で問題が起きても、いつ誰が何をした結果エラーになったのかを追えません。

### Step 10: README整理

**なぜ行うのか**

起動方法、環境変数、API、テスト、構成を文書化し、他の人や将来の自分がプロジェクトを再現できるようにするためです。

**行わないとどうなるか**

環境構築や操作方法が属人化し、数か月後には自分でも起動手順を思い出せなくなります。

## Azure

### Step 1: Azure基礎

**なぜ行うのか**

サブスクリプション、リソースグループ、リージョンを理解し、クラウドリソースを整理して配置するためです。

**行わないとどうなるか**

リソースの所有者、配置先、課金対象が分からなくなり、不要なリソースの削除や障害対応が難しくなります。

### Step 2: コスト管理

**なぜ行うのか**

無料枠、予算、超過課金、スケール設定を確認し、学習中の意図しない課金を抑えるためです。

**行わないとどうなるか**

使っていないリソースが稼働し続けたり、無料枠を超えたことに気づかなかったりして、想定外の請求につながります。

### Step 3: Azure SQL Database

**なぜ行うのか**

ローカルDBではなく、公開APIから利用できるクラウドDBを作成するためです。

**行わないとどうなるか**

Container AppsからローカルPCのDBへ接続できず、公開APIのデータ保存先がありません。

### Step 4: Azure SQL Migration

**なぜ行うのか**

ローカルで定義したEntityとMigrationをAzure SQLへ適用し、アプリが必要とするテーブル構造をクラウド上にも作るためです。

**行わないとどうなるか**

APIはAzure SQLへ接続できても、必要なテーブルが存在せず、登録・ログイン・予約処理が失敗します。

### Step 5: Docker Hub

**なぜ行うのか**

ビルドしたDocker ImageをContainer Appsが取得できる場所へ公開し、タグでバージョンを管理するためです。

**行わないとどうなるか**

Azure Container Appsに実行させるImageを渡せず、更新したAPIをデプロイできません。

### Step 6: Container Apps公開

**なぜ行うのか**

Docker ImageをAzure上で実行し、HTTPSのURL経由でAPIを利用できるようにするためです。シークレットもAzure側で安全に渡します。

**行わないとどうなるか**

APIはローカルでしか使えず、外部からSwaggerやAPIクライアントで利用・検証できません。秘密情報をImageやコードに埋め込む危険も残ります。

### Step 7: Application Insights

**なぜ行うのか**

公開後のHTTPリクエスト、エラー、処理時間、Azure SQLへの依存関係、アプリケーションログを観測するためです。

**行わないとどうなるか**

公開APIで遅延やエラーが起きても、どのAPI・DB接続・アプリログに問題があるのか判断できません。

### Step 8: GitHub Actions CI/CD

**なぜ行うのか**

mainブランチへのpushを起点に、buildとtestが成功したImageだけをDocker Hubへpushし、Container Appsへ更新するためです。OIDCによりAzure認証情報も安全に扱います。

**行わないとどうなるか**

Docker build、push、Azure更新を毎回手動で行う必要があります。テストに失敗したコードを誤って公開したり、認証情報を長期間の秘密値として保存したりするリスクが高まります。

### Step 9: Azure README

**なぜ行うのか**

Azure構成、シークレット方針、Migration、監視、CI/CD、コスト管理をREADMEへ集約し、公開後の運用を再現できるようにするためです。

**行わないとどうなるか**

クラウド構成の意図や復旧手順が分からなくなり、別の人が引き継ぐことも、不要になったリソースを安全に片付けることも難しくなります。

## 振り返りで重要なこと

### つまずきと解決の考え方

| 起きたこと | 原因 | 解決・今後の確認方法 |
| --- | --- | --- |
| Dockerfileの`COPY`でビルド失敗 | JSON形式の`COPY`記法が正しくなく、`.csproj`をコピーできなかった。 | Dockerfileは`COPY ["ファイル名", "./"]`のようにJSON配列の要素を正しく区切る。まず`dotnet build`が成功することも確認する。 |
| `dotnet test`で属性重複やxUnit未検出 | テストプロジェクトをAPIプロジェクト配下に置いたことで、API側がテストの`obj`などを誤ってコンパイル対象にした。 | `src`と`tests`を分離し、ソリューションのプロジェクトパスを正しく設定する。 |
| Docker ComposeでAPIがDBへ接続できない | コンテナ内の`localhost`はホストPCではなく、そのコンテナ自身を指す。 | Compose内のAPIからDBへ接続するときは`db`のようなサービス名を接続先に使う。 |
| Container Appsが起動直後に終了 | DB接続文字列やJWT秘密鍵など、起動時に必須の環境変数が未設定だった。 | リビジョンログを最初に確認し、Container Appsのシークレットと環境変数の対応を確認する。 |
| GitHub ActionsのAzureログイン失敗 | OIDCのフェデレーション資格情報と、Workflowが発行したsubjectが一致していなかった。 | Azureアプリ登録、GitHub Organization / Repository / Branch、subjectを対応させる。 |
| GitHub Actionsのdeployが開始しない | GitHub-hosted Runner側の障害でRunnerを取得できなかった。 | buildや設定を疑う前に、ジョブのログとGitHub Statusを確認する。障害解消後に再実行する。 |

### 実務で使う設計判断

| 判断 | このプロジェクトでの適用 |
| --- | --- |
| ControllerはHTTPの入口に限定する | Controllerはリクエストを受け、認証済みユーザー情報を取得し、Serviceを呼び出す。 |
| Serviceへ業務ロジックを集約する | 認証、予約重複判定、キャンセル可否、Admin操作を`AuthService`や`ReservationService`へ置く。 |
| DTOでAPIの入出力を固定する | Entityをそのまま外部へ公開せず、Request / Response DTOで受け取る値と返す値を定義する。 |
| enumで状態と権限を表現する | `UserRole`と`ReservationStatus`で、許可される操作と予約の状態遷移を明確にする。 |
| 物理削除より状態管理を使う | 予約は削除せず`Cancelled`へ変更し、履歴とキャンセル済み状態を残す。 |
| 失敗をHTTPステータスへ変換する | 業務例外をMiddlewareで`400`、`404`、`409`などの`ProblemDetails`へ統一する。 |
| テストをデプロイ前の条件にする | GitHub Actionsではbuildとtestが成功した場合だけdeployジョブを実行する。 |

### 秘密情報を扱うルール

| 場所 | 管理するもの | 注意点 |
| --- | --- | --- |
| `.env` | Docker Composeのローカル用パスワードやJWT設定 | `.gitignore`に含め、`.env.example`にはダミー値だけを書く。 |
| User Secrets | ローカルからAzure SQLへMigrationを適用するときの接続文字列 | 開発PCだけに保存し、Gitへ含めない。 |
| Container Apps Secrets | Azure SQL接続文字列、JWT秘密鍵、初期Admin情報、Application Insights接続文字列 | シークレットを環境変数として参照し、実値をポータル画面やログへ出さない。 |
| GitHub Secrets | Azure OIDCのID情報、Docker Hubユーザー名・トークン | Workflow内では`${{ secrets.NAME }}`で参照し、値を`echo`しない。 |

トークンやパスワードを誤って画面共有・コミット・ログ出力した場合は、削除だけで終わらせず、直ちに失効させて新しい値へ再発行します。

### 最終到達状態

- ローカルでは`docker compose up --build`でAPIとSQL Serverが起動し、Swaggerから予約APIを確認できる。
- `dotnet test ReservationManagerAPI2.sln`でReservationServiceの単体テストが成功する。
- GitHub Actionsで`restore`、`build`、`test`が自動実行される。
- `main`ブランチへのpush後、成功したDocker ImageがDocker Hubへpushされ、Azure Container Appsが新しいリビジョンへ更新される。
- Azure上のSwaggerからログイン、JWT認証、予約の保存・取得を確認できる。
- Application InsightsとLog Analyticsで、HTTPリクエスト、Azure SQL依存関係、業務ログ、コンテナログを確認できる。

### 今後の発展候補

- `WebApplicationFactory`を使った統合テストを追加し、HTTP経由で認証・認可・予約作成を検証する。
- 本番環境用の設定を追加し、Swagger公開方針、CORS、HTTPS、ログレベルを環境ごとに整理する。
- APIの入力検証を強化し、DTOのValidation属性やFluentValidationを学習する。
- Azureリソースの削除・復旧手順をREADMEへ追加し、学習終了後のコストを確実に止められるようにする。

## 次回プロジェクト開始時に必ず読むこと

この章は、次のAPIを作るときに、完成コードを先に見る前のチェックリストとして使う。

### ステップを始める前の確認質問

新しい機能を作る前に、まず次の質問へ自分の言葉で答える。

```text
1. このステップで解決したい問題は何か？
2. この処理は Controller、Service、DbContext、DTO のどこに置くべきか？
3. これを実装しないと、利用者・運用者・開発者に何が困るか？
4. APIの入力は何で、出力は何か？
5. 成功をどの方法で確認するか？
6. 失敗した場合、どのHTTPステータスを返すべきか？
```

答えられない項目がある場合は、完成コードを求める前に、その項目の考え方を調べるか質問する。

### 見ないで説明できるようにする基本形

次の項目は、記法を完全に暗記するよりも、役割・配置先・必要な理由を自分で説明できる状態を目標にする。

| 項目 | 自分で説明したいこと |
| --- | --- |
| Entity、DTO、Service、Controller、DbContext | それぞれの責務と、なぜ業務ロジックをServiceへ置くか。 |
| DI と `AddScoped` | ServiceやDbContextを必要な場所へ注入する理由。 |
| Request DTO と Response DTO | APIが受け取る値と返す値をEntityから分ける理由。 |
| `async` / `await`、`AnyAsync`、`SaveChangesAsync` | DB操作を非同期にする理由と、各メソッドの用途。 |
| JWT、`[Authorize]`、Role認可 | 認証と認可の違い、UserとAdminの操作を分ける理由。 |
| HTTPステータス | `400`、`401`、`403`、`404`、`409`、`500`の意味と使い分け。 |
| Middleware | 例外処理を一箇所へ集約する理由。 |
| テストの Arrange / Act / Assert | テストデータ準備、実行、検証を分ける理由。 |
| Dockerfile、Image、Container、Compose | ローカルと本番の実行環境を揃えるための役割。 |
| Docker内の接続先 | コンテナ内の`localhost`とComposeサービス名の違い。 |
| 環境変数とシークレット | 接続文字列や秘密鍵をコードとGitから分離する理由。 |
| CI/CD | `build`、`test`、`deploy`を順番に行う理由。 |

### 実務では調べながらでよいもの

以下は暗記対象ではない。公式ドキュメントを参照し、現在使用しているバージョンに合わせて設定する。

- NuGetパッケージ名とバージョン
- JWTの`TokenValidationParameters`の詳細設定
- Dockerfileの細かい書式と最適化
- SQL ServerやAzure SQLの接続文字列
- `dotnet ef`コマンドの細かいオプション
- GitHub Actions、Docker、AzureのYAML設定
- Azure Portalの画面操作
- OpenTelemetry、Application Insightsの設定値

### 各機能の完了条件

新しい機能やプロジェクトは、「コードを書いた」だけで完了にしない。次を確認して完了とする。

```text
- APIが起動する
- SwaggerまたはAPIクライアントで成功ケースを確認した
- 失敗ケースもHTTPステータスとレスポンス本文まで確認した
- 重要な業務ルールに単体テストがある
- docker composeでローカル環境を再現できる
- CIでbuildとtestが通る
- 秘密情報がGit、README、ログに含まれていない
- READMEだけで起動方法と構成が分かる
```

### ステップ後の自己評価

各ステップを終えたら、次のいずれかを記録する。分からないまま先へ進んでいないかを確認するための基準にする。

| 評価 | 判断基準 | 次にすること |
| --- | --- | --- |
| 自分で説明できる | なぜ必要か、どこへ書くか、確認方法を説明できる。 | 次のステップへ進む。 |
| 調べれば実装できる | 役割は理解しているが、細かい記法や設定値は調べる必要がある。 | 公式ドキュメントを参照して実装する。 |
| まだ分からない | 完成コードを写しただけで、理由や失敗時の影響を説明できない。 | コードを書く前に、目的・責務・確認方法を説明し直す。 |

### AIを使うときの順番

次回は、最初から完成コードを求めない。次の順番でAIを使う。

1. 自分で上の「ステップを始める前の確認質問」に答える。
2. 分からない部分だけ、考え方・責務・確認方法を質問する。
3. 自分で実装する。
4. エラーが出たら、エラー全文、該当コード、自分の予想原因を添えて質問する。
5. 完成後に、設計・例外処理・テスト不足をレビューしてもらう。

この順番にすると、AIは答えを写すための道具ではなく、理解と確認を補助する道具になる。
